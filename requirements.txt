anthropic>=0.40
# Direct dependency since llm.py talks to OpenAI-compatible endpoints itself. Already
# present transitively via anthropic; pinned here because we import it by name.
httpx>=0.27
docxtpl>=0.17
python-docx>=1.1
docx2pdf>=0.1.8
pypdf>=5.0
pydantic>=2.9
python-dotenv>=1.0
pytest>=8.0
# Web UI (src/resume_tailor/web). Not required for the CLI.
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
